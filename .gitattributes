# Normalize line endings to LF in the repo — this is Linux-targeted tooling
# (install.sh and friends must stay LF regardless of the checkout OS).
* text=auto eol=lf
*.sh   text eol=lf
*.py   text eol=lf
*.j2   text eol=lf
*.md   text eol=lf
